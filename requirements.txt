# Core backend — unpinned minimums chosen so that wheels exist for Python 3.14 on Windows
fastapi>=0.118.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.1
python-multipart>=0.0.17
pydantic>=2.10.0

# RAG pipeline
# sentence-transformers pulls in torch; torch>=2.6 is required for Python 3.14 wheels.
sentence-transformers>=3.3.0
faiss-cpu>=1.9.0
requests>=2.32.3

# Document processing
PyMuPDF>=1.25.0

# ML / Training
# scikit-learn>=1.6 ships wheels for newer Pythons.
# NOTE: LogisticRegression(multi_class=...) was removed in sklearn 1.7 —
# training/scripts/train.py no longer passes that argument.
scikit-learn>=1.6.0
joblib>=1.4.2
pandas>=2.2.3
numpy>=2.1.0

# Evaluation / Visualization
matplotlib>=3.9.2
seaborn>=0.13.2

# HTTP
httpx>=0.27.2
